<Window
    x:Class="WpfApp1.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WpfApp1"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainWindow"
    Width="800"
    Height="450"
    Loaded="Window_Loaded"
    mc:Ignorable="d">
    <Window.Background>
        <ImageBrush ImageSource="refs/BG-1_1332x850.jpg" />
    </Window.Background>

    <Grid>
        <StackPanel
            x:Name="Auth_Panel"
            Width="400"
            Height="250"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Background="AntiqueWhite">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <TextBlock
                    Grid.Row="0"
                    FontSize="26"
                    Text="Форма авторизации" />
                <GroupBox
                    Grid.Row="1"
                    Background="White"
                    BorderBrush="Black"
                    FontSize="22"
                    Header="Логин">
                    <TextBox BorderBrush="Transparent" FontSize="22" />
                </GroupBox>
                <GroupBox
                    Grid.Row="2"
                    Background="White"
                    BorderBrush="Black"
                    FontSize="22"
                    Header="Пароль">
                    <PasswordBox BorderBrush="Transparent" FontSize="22" />
                </GroupBox>
                <Button
                    Grid.Row="3"
                    Margin="0,10"
                    Background="White"
                    Content="Войти"
                    FontSize="22" />
            </Grid>

        </StackPanel>
    </Grid>
</Window>
